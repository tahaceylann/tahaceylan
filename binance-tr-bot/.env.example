# Binance TR (or any Binance-API-compatible exchange) credentials.
# NEVER commit your real .env file. Copy this to .env and fill in your values.

BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# Base REST URL of the exchange. Binance TR historically exposed a
# Binance-compatible REST API at https://www.binance.tr/apiproxy/v3/api ;
# verify the current endpoint with your exchange before trading live.
BINANCE_BASE_URL=https://www.binance.tr/apiproxy/v3/api

# Trading pair, e.g. BTCTRY, ETHTRY, BTCUSDT
SYMBOL=BTCTRY

# Candle interval: 1m,3m,5m,15m,30m,1h,4h,1d ...
INTERVAL=15m

# Strategy parameters
FAST_MA=9
SLOW_MA=21
RSI_PERIOD=14
RSI_OVERBOUGHT=70
RSI_OVERSOLD=30

# Risk management
QUOTE_ORDER_SIZE=100          # amount of quote currency (e.g. TRY) to spend per buy
MAX_OPEN_POSITIONS=1
STOP_LOSS_PCT=0.02            # 2%
TAKE_PROFIT_PCT=0.04          # 4%
TRAILING_STOP_PCT=0.015       # 1.5% trailing stop once in profit
MAX_DAILY_LOSS_PCT=0.05       # kill-switch: stop trading if daily loss exceeds 5%

# Safety
DRY_RUN=true                  # true = paper trading (no real orders sent)
POLL_SECONDS=30

# Logging / persistence
STATE_FILE=state.json
LOG_LEVEL=INFO

# Optional: Telegram notifications
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
